<resources>
    <!-- System splash screen theme — shown before Compose is up.
         core-splashscreen backports this to API 26+. -->
    <style name="Theme.Weather.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Weather</item>
    </style>

    <!-- Base theme for the Compose-only app. No ActionBar — Compose Scaffold handles navigation. -->
    <style name="Theme.Weather" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
